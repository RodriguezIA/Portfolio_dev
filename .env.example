# Backend proxy URL — points to the chat API server (no AI keys exposed to the browser)
# Local dev: http://localhost:3000
# Production: https://api-portafolio.edgardev.mx
PUBLIC_API_BASE=http://localhost:3000
